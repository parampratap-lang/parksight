# This public repo ships CODE ONLY. The raw 109MB CSV and the derived
# backend/artifacts/*.json are git-ignored (police-data sensitivity).
#
# To run with data: drop your source CSV here as `violations.csv`, then:
#   ../../.venv/bin/python ../../backend/build/build_artifacts.py
# That regenerates backend/artifacts/*.json and the app comes alive.
